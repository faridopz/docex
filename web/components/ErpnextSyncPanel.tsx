"use client";

// Superseded by the provider-agnostic IntegrationsPanel. Kept as a thin
// re-export so any lingering import keeps compiling. Safe to delete.
export { IntegrationsPanel as ErpnextSyncPanel } from "@/components/IntegrationsPanel";
